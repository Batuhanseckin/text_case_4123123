part of city_detail_view;

class _CityDetialDesktop extends StatelessWidget {
  const _CityDetialDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
