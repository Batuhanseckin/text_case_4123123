part of city_detail_view;

class _CityDetailMobile extends StatelessWidget {
  const _CityDetailMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
